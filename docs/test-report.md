# 신뢰성 시험 보고서 — orbit-pass-sim

- 일자: 2026-09-15
- 대상: `src/main/java/orbitsim` (시험 계획 [`test-plan.md`](test-plan.md) §1)
- 기준: [`requirements.md`](requirements.md) REQ 24개 · QR-01~06
- 환경: 로컬 Windows 11 · Temurin JDK 21.0.12 / GitHub Actions ubuntu·windows × JDK 21 ([run 34954954787](https://github.com/Haejyn/orbit-pass-sim/actions/runs/34954954787) 두 OS 모두 성공) / Jenkins LTS 로컬 (빌드 #2 성공, 7 스테이지 73 s, [콘솔 로그](evidence/2026-09-15-jenkins-build-2-console.log) · [화면](evidence/2026-09-15-jenkins-job.png))
- 도구: JUnit 5.13 · JaCoCo 0.8.13 · PIT 1.20.1 · PMD 7.14 · SpotBugs 4.9.3 · SonarQube Cloud

## 1. 요약

| 항목 | 기준 | 결과 | 판정 |
|---|---|---|---|
| 동적 시험 | 실패 0 | **126 / 126 통과** (9 클래스) | 합격 |
| 라인 커버리지 | ≥ 90 % | **92.3 %** (192/208, 데모 `Main` 제외 시 192/193) | 합격 |
| 분기 커버리지 | ≥ 85 % | **91.2 %** (93/102) | 합격 |
| 뮤테이션 검출률 | ≥ 80 % | **92.4 %** (231/250) — 생존 19개 전부 동등 뮤턴트로 판정, §4 | 합격 |
| 요구사항 추적 | 전 REQ 검증 | **24 / 24** ([`traceability.md`](traceability.md)) | 합격 |
| PMD · SpotBugs | 0 | 0 · 0 | 합격 |
| 컴파일 경고 | 0 (`-Werror`) | 0 | 합격 |
| 발견 결함 | — | **제품 결함 2건 (D-4·D-5) 수정**, 시험 결함 3건 (D-1~D-3) 수정 | — |

## 2. 제품 결함

두 결함 모두 **성질 기반 무작위 시험**(`RobustnessTest`, 고정 시드)이 찾았다. 기존 예제 기반 시험 73개는 모두 통과하던 상태였다.
수정 전 실행 기록: [`evidence/2026-09-15-red-run-D4-D5.log`](evidence/2026-09-15-red-run-D4-D5.log) (126 중 9 실패) → 수정 후 [`evidence/2026-09-15-green-run.log`](evidence/2026-09-15-green-run.log).

### D-4 · 케플러 풀이 비수렴 — 장기 전파에서 예외 (심각도: 높음)

| 항목 | 내용 |
|---|---|
| 증상 | `ArithmeticException: Kepler solver did not converge` |
| 재현 | a = 12,233 km, e = 0.24 궤도를 **1년** 전파 (M ≈ 1.5×10⁴ rad). 무작위 입력으로는 \|M\| ≥ 10⁶ 에서 약 16 % 실패 (2,000 건 중 311~352) |
| 원인 | 뉴턴 보정의 수렴 기준이 절대값 1e-12 인데, \|M\| 이 크면 인접한 double 사이 간격(ulp)이 이보다 커진다. 보정량이 두 값 사이를 오가며 기준 아래로 내려가지 않아 반복 상한(50)에 도달 |
| 영향 | 운용 기간이 긴 위성의 패스 예측이 **입력에 따라 간헐적으로** 실패. 짧은 시험 창에서는 드러나지 않음 |
| 수정 | 해의 2π 주기성 E(M + 2πk) = E(M) + 2πk 를 이용해 M 을 [−π, π] 로 줄여 풀고 회전수를 되돌림 |
| 확인 | \|M\| 10³~10¹⁵ × 2,000 건씩, 1·30·1,000 년 전파 × 500 궤도 — 예외 0, 비에너지 상대오차 < 1e-9 (REQ-KEP-01 · REQ-PRP-05) |

### D-5 · 각도 정규화가 2π 반환 — 방위각 360.0° (심각도: 중간)

| 항목 | 내용 |
|---|---|
| 증상 | `normalizeAngle(-1e-16)` = 6.283185307179586 (= 2π). 계약은 [0, 2π) |
| 재현 | 정북에서 아주 조금 서쪽(atan2 가 −1e-23 반환)에 있는 위성의 방위각이 **360.0°** |
| 원인 | x + 2π 가 반올림돼 정확히 2π 가 됨. 기존 시험은 −1e-9 로만 확인해 드러나지 않았다 (−1e-9 + 2π 는 2π 보다 작게 표현됨) |
| 영향 | 방위각을 [0, 360) 로 가정하는 안테나 구동·표시 로직에서 경계 처리 오류 가능 |
| 수정 | 결과가 2π 이상이면 0 으로 접음 |
| 확인 | 적대적 경계 입력 11개(−Double.MIN_VALUE, −ulp(2π), −0.0 …) + 무작위 100,000 건 + 관측각 10,000 건 (REQ-FRM-03 · REQ-GST-02) |

## 3. 시험 결함 (첫 실행)

| # | 실패 | 원인 | 조치 |
|---|---|---|---|
| D-1 | 케플러 기대값 M=2.0, e=0.9 → 2.6913 기대, 실제 2.5224 | **기대값이 틀렸다** (검증 없이 적은 값) | Python 이분법으로 1e-15 까지 풀어 기대값 재작성, 대조점 4 → 6개 |
| D-2 | 비에너지 보존 e=0.9 에서 생성 예외 | 시험 궤도 근지점 2,000 km — **지구 안** | 장반경 80,000 km (모든 e 에서 유효) |
| D-3 | 각운동량 보존 e=0.8 에서 같은 예외 | 근지점 3,000 km | 장반경 40,000 km |

## 4. 커버리지·뮤테이션 분석

**1차**(예제 시험만): 분기 88.0 %, 뮤테이션 88 % (216/245) → 놓친 분기와 생존 뮤턴트를 줄 단위로 판정해 보강했다.

| 발견 | 판정 | 조치 |
|---|---|---|
| 경도 하한 · 고도 NaN · 마스크 하한 분기 미실행 | **경계값 시험 누락** | 경계·비유한값 시험 추가 |
| `GroundStation` ENU 식의 부호를 바꿔도 시험 통과 | **시험 약점**: 적도·경도 0° 지상국만 써서 sin 항이 0 | 대전(36°N, 127°E)에서 외적으로 따로 만든 동·북·천정 기저와 대조 |
| 패스 최대 고도각 추적 조건을 지워도 통과 | **시험 약점**: 최대 고도각 값을 검증하는 시험 없음 | 1 s 간격 정밀 탐색과 대조 (±0.05°) |
| `end − start` 를 `end + start` 로 바꿔도 통과 | **시험 약점**: 창 시작이 항상 0 | 시작 ≠ 0 인 간격 검사 추가 |

**최종 생존 뮤턴트 19개 판정** (2차 20개 중 `end + start` 1개는 시험 추가로 제거, 나머지는 근거를 남기고 시험을 추가하지 않음):

| 위치 | 뮤턴트 | 판정 | 근거 |
|---|---|---|---|
| `TwoBodyPropagator` L42·43 | `ci·y − si·z` ↔ `+` | 동등 | 근점 좌표계(PQW)에서 z = 0 이 항상 성립 |
| `KeplerSolver` 초기값 `e < 0.8` 경계·제거 | 동등 | 초기값만 달라지고 같은 해로 수렴 (e < 1 전 범위 시험) |
| `KeplerSolver` `\|δ\| < TOL` ↔ `≤`, 반복 상한 경계 | 동등 | 수렴 판정이 한 반복 늦어질 뿐 |
| `Frames` `x < 0` ↔ `≤` | 동등 | x = 0 이면 +2π 뒤 곧바로 0 으로 접힘 |
| `PassPredictor` `el > max` ↔ `≥`, `isVisible` `≥` ↔ `>`, 이분법 종료 경계 | 동등 | 실수 동치(측도 0) 또는 반복 1회 차이 |
| `PassPredictor` 초기 `aos`·`maxElev` 조건 제거, 비가시 구간 추적 조건 제거 | 동등 | 값이 AOS 시점에 다시 덮어써져 결과에 쓰이지 않음 |
| `PassPredictor` `!(end > start)` 경계 | 동등 | 뒤따르는 간격 검사가 같은 입력을 거부 |
| `PassPredictor` 루프 `t ≤ end + 1e-9` 변형 | 동등 | 마지막 표본이 창 끝과 겹치는 경우만 달라지고, 그때도 창 끝 절단 규칙이 같은 결과를 냄 |

## 5. 정적 분석

| 도구 | 결과 | 판단 |
|---|---|---|
| PMD quickstart | 첫 실행 18건 → 0 | 중괄호 없는 `if` 11 · 한 줄 다중 선언 5 는 수정. `LogicInversion` 2 는 **규칙 제외** — `!(end > start)` 는 NaN 도 거부하려는 의도이고 권고대로 `end <= start` 로 쓰면 NaN 이 통과한다. 근거를 규칙 파일·코드 주석에 남김 |
| SpotBugs `-effort:max -low` | 0 | — |
| SonarQube Cloud | CI 잡 구성 완료, **아직 분석 전** — `SONAR_TOKEN` 미등록으로 잡이 건너뜀 (run 34954954787) | 토큰 등록 후 첫 품질 게이트 결과를 여기에 기록 |

## 6. 파이프라인 구축 중 해결한 환경 문제

| # | 문제 | 원인 | 해결 |
|---|---|---|---|
| E-1 | SpotBugs `Unsupported class file major version 70` | 로컬 기본 JVM 이 Java 26, SpotBugs 4.9 는 Java 24 까지 | 스크립트가 JVM 버전 감지 후 건너뛰고 사유 출력, 실행은 JDK 21 |
| E-2 | Jenkins 첫 빌드 체크아웃 실패 `.git/hooks/: Filename too long` | Windows 경로 260자 제한 — `JENKINS_HOME\workspace\job@script\<64자 해시>\.git\hooks` | JENKINS_HOME 을 짧은 경로로 이동 |
| E-3 | 골든 데이터 재현성 검사가 OS 따라 흔들릴 위험 | `csv` 모듈 기본 CRLF, OS 별 libm sin·cos 마지막 자리 차이 | 줄바꿈 LF 고정(`.gitattributes`), 문자열 비교 대신 수치 허용 오차(1e-6 km) 비교 |
| E-4 | GitHub Actions **windows-latest 에서만** 추적 단계 실패 `UnicodeEncodeError: 'charmap' codec can't encode '→'` (ubuntu·로컬·Jenkins 는 통과) | Windows 러너 콘솔 기본 인코딩 cp1252 — 보고 문자열의 `→`·`✗` | 스크립트가 stdout 을 UTF-8 로 재설정. OS 매트릭스를 둔 이유가 실제로 드러난 사례 |

## 7. 한계와 다음 시험

- 섭동 없는 이체 모델 → 공개 TLE 로 SGP4 결과와 패스 시각 대조
- 성능·장시간 창(수십 일) 시험, 메모리 사용량 측정
- C# 이식본에 같은 요구사항·시험 적용 (xUnit · coverlet) 해 언어 간 결과 대조
